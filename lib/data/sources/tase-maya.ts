// TASE Maya API stub — returns null until real API integration is wired up.
// The research pipeline treats all null returns gracefully.

export async function getTaseCompanyByTicker(_ticker: string): Promise<null> {
  return null;
}

export async function fetchRecentFilings(_ticker: string): Promise<unknown[]> {
  return [];
}
